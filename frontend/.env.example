# ─── VegiX Frontend — Environment Variables ──────────────────────────────────
# Copy this file to .env and adjust for your environment. NEVER commit .env to git!

# For production (single-domain): Leave empty to use relative paths (nginx proxy)
VITE_API_URL=

# For development with direct backend access:
# VITE_API_URL=http://localhost:5000
